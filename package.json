{
  "name": "eden-ai-sustainability-api",
  "version": "1.0.0",
  "description": "FastAPI server for Eden AI Sustainability Counter",
  "scripts": {
    "start": "uvicorn server:app --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "python": "3.11"
  }
}
